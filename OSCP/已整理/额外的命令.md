额外的命令

获取MD5哈希值：md5sum

检查端口状态：netstat -tuln 

检查1200端口状态：netstat -tuln | grep 1200

禁止同步时间

vmtoolsd --cmd "timesync.disable"

sudo timedatectl set-ntp false

sudo systemctl stop systemd-timesyncd

sudo systemctl disable systemd-timesyncd

查询同步时间状态

timedatectl

vmtoolsd --cmd "timesync.status"