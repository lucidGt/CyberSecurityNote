# 🧭 OSCP TODO LIST（可勾选：Markdown Checkboxes）

> 说明：在支持任务列表的 Markdown 渲染器中可点击勾选；不支持的环境可手动把 [ ] 改成 [x]。

---

## ✅ 第一层：必须搞定

### 🌐 HTTP / HTTPS（80 / 443 / 8080）

- [x] 会跑基础 Web 枚举流程（nmap + 目录爆破 + 手工浏览）
- [x] 目录爆破（ffuf / dirsearch）能读懂结果
- [x] SQL 注入：至少 1 次完整链（注入 → 枚举 → 拿数据）
- [x] 文件上传：至少 1 次（上传 → WebShell/RCE）
- [x] LFI：能读 `/etc/passwd`（最好 1 次 LFI → RCE）
- [x] 命令注入：至少命中 1 次（`; && | || $( )`）
- [x] 登录绕过 / 逻辑漏洞：至少练 1 次
- [ ] 整理一页 Web Cheat Sheet（入口 → payload → 下一步）

### 🗂 SMB（445 / 139）

- [ ] 会枚举共享（匿名 / 有凭据）
- [ ] 会下载共享里的文件（配置 / 备份 / 脚本）
- [ ] 会枚举用户 / 组 / 域信息
- [ ] 至少 1 次：SMB 文件 → 拿到凭据
- [ ] 至少 1 次：SMB → SSH / RDP / WinRM 登录
- [ ] 整理一页 SMB Cheat Sheet

### 🔐 SSH（22）

- [x] 会判断认证方式（password / publickey）
- [x] 至少 1 次：用“从别处拿到的凭据”登录 SSH
- [x] 至少 1 次：用私钥（`ssh -i`）登录
- [ ] 会用 `scp` 传文件
- [ ] SSH 登录后能立刻跑 Linux 提权流程

### 📁 FTP（21）

- [ ] 会检查 anonymous 登录
- [ ] 会列目录、下载文件
- [ ] 至少 1 次：FTP 下载源码/备份 → 拿线索
- [ ] 如果允许上传：练 1 次上传利用
- [ ] 整理一页 FTP Cheat Sheet

### 🧠 Linux / Windows 提权（必须）

#### Linux 提权

- [x] 背熟提权顺序（sudo → 凭据 → SUID → cron → PATH → cap → 服务 → 内核）
- [x] sudo 提权至少 1 次
- [ ] SUID / cron / PATH 任一方式至少 2 次
- [x] 累计 ≥ 3 台 Linux 提权成功

#### Windows 提权

- [ ] 会手工枚举服务权限
- [ ] Unquoted Service Path / 弱服务权限 至少 1 次
- [ ] SeImpersonate / Potato 系列：至少了解 1 次（能复现更好）
- [ ] 累计 ≥ 2 台 Windows 提权成功

---

## 🟡 第二层：应该掌握

### 🖥 RDP（3389）

- [ ] 会用 `xfreerdp` 登录
- [ ] 至少 1 次：RDP 登录 → Windows 提权

### ⚙ WinRM（5985 / 5986）

- [ ] 会用 `evil-winrm` 连接
- [ ] 至少 1 次：WinRM → SYSTEM

### 🗄 数据库服务

- [x] MySQL（3306）：会连接、枚举、找敏感数据
- [ ] MSSQL（1433）：会基础枚举（版本/库/用户）
- [ ] Postgres（5432）：会连接和基础查询

### 📬 SMTP（25）

- [ ] 会做用户枚举
- [ ] 至少 1 次：SMTP 用户 → 其他服务利用

### 📡 SNMP（161）

- [ ] 会用 `snmpwalk`
- [ ] 至少 1 次：SNMP 泄露信息 → 有效攻击线索

---

## 🟢 第三层：遇到再学

- [ ] NFS（2049）：会挂载共享 + 权限判断
- [ ] Redis（6379）：会判断无认证 + 基础利用思路
- [ ] Tomcat：识别 manager / WAR 利用流程
- [ ] Jenkins：识别未授权 / Script Console → RCE 思路

---

## 🧪 每台靶机固定流程 TODO（每台都建议打勾）

- [ ] nmap 全端口扫描完成
- [ ] 至少 3 个服务做过枚举
- [ ] 找到入口点
- [ ] 拿到初始 shell
- [ ] 完成 Linux / Windows 提权
- [ ] 写 ≥10 行攻击链复盘（入口→漏洞→shell→提权）
- [ ] 标记报告截图点

---

## 🎯 自测“是否搞定一个服务”

- [ ] 看到端口，30 秒内能说出 3 个枚举动作
- [ ] 10 分钟内能完成枚举并理解结果
- [ ] 能把线索接到：Shell / 凭据 / 提权路径